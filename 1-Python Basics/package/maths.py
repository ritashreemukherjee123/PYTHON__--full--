def addition(a,b):
    return a+b

### __init__.py can only make package folder a package, then the modules and functions could be called in other files. It does not have any relation with the functions defined in the modules.